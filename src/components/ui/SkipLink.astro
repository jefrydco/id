---

---

<a href="#main-content" class="skip-link" data-pagefind-ignore="all">
  Skip to main content
</a>

<style>
  .skip-link {
    position: absolute;
    top: -100%;
    left: 1rem;
    z-index: 9999;
    padding: 0.75rem 1rem;
    background: var(--bg);
    color: var(--text-primary);
    border: 2px solid var(--text-primary);
    border-radius: 0.25rem;
    text-decoration: none;
    font-weight: var(--font-weight-bold);
    transition: top 0.2s ease;
  }

  .skip-link:visited {
    color: var(--text-primary);
  }

  .skip-link:hover {
    color: var(--text-primary);
    opacity: 0.8;
  }

  .skip-link:focus {
    top: 1rem;
    outline: none;
  }
</style>
