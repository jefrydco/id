---
import type { CollectionEntry } from "astro:content";
import FormattedDate from "@/components/widgets/FormattedDate.astro";
import { defaultLocale, getTranslations, type Locale } from "@/i18n";
import { getNoteSlug } from "@/utils/i18n-notes";

interface Props {
	notes: CollectionEntry<"notes">[];
	locale?: Locale;
}

const { notes, locale = defaultLocale } = Astro.props;
const t = getTranslations(locale);

function getNoteUrl(note: CollectionEntry<"notes">): string {
	const slug = getNoteSlug(note);
	return locale === "en" ? `/notes/${slug}/` : `/${locale}/notes/${slug}/`;
}
---

{
  notes.length > 0 && (
    <section class="related-notes" aria-labelledby="related-notes-heading" data-pagefind-ignore="all">
      <h2 id="related-notes-heading" class="related-notes-heading">
        {t.ui.relatedNotes}
      </h2>
      <ul class="related-notes-list">
        {notes.map((note) => (
          <li>
            <a href={getNoteUrl(note)}>
              <div class="note-item">
                <p class="title">{note.data.title}</p>
                <div class="dotted-divider" />
                <p class="date font-features">
                  <FormattedDate date={note.data.publishedAt} context="list" locale={locale} />
                </p>
              </div>
            </a>
          </li>
        ))}
      </ul>
    </section>
  )
}

<style>
  .related-notes {
    margin-top: 3rem;
  }

  .related-notes-heading {
    font-size: var(--font-size-m);
    font-weight: var(--font-weight-bold);
    line-height: 1.75;
    margin: 3.75em 0 1.75em 0;
  }

  .related-notes-list {
    padding: 0;
    margin: 0;
    list-style: none;
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .related-notes-list li {
    list-style: none;
    margin: 0;
    padding: 0;
  }

  .related-notes-list li::before,
  .related-notes-list li::marker {
    display: none;
    content: none;
  }

  .related-notes-list a {
    color: var(--text-primary);
    display: block;
    text-decoration: none;
    transition: opacity 0.15s ease-out;
  }

  .related-notes-list a:focus-visible {
    outline: 2px solid var(--focus);
    outline-offset: 2px;
    border-radius: 0.125rem;
  }

  @media (hover: hover) and (pointer: fine) {
    .related-notes-list:hover a {
      opacity: 0.4;
    }

    .related-notes-list:hover a:hover {
      opacity: 1;
    }

    .related-notes-list:hover a:hover .dotted-divider {
      color: var(--text-secondary);
    }

    .related-notes-list:hover a:hover .date {
      color: var(--text-secondary);
      opacity: 1;
    }
  }

  .related-notes-list:has(a:focus-visible) a:not(:focus-visible):not(:hover) {
    opacity: 0.4;
  }

  .related-notes-list:has(a:focus-visible) a:focus-visible .dotted-divider {
    color: var(--text-secondary);
  }

  .related-notes-list:has(a:focus-visible) a:focus-visible .date {
    color: var(--text-secondary);
    opacity: 1;
  }

  .note-item {
    height: 2.75rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    gap: 0.75rem;
  }

  .title {
    margin: 0;
    flex-shrink: 1;
    min-width: 0;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
  }

  .date {
    margin: 0;
    color: var(--text-secondary);
    opacity: 0.75;
    letter-spacing: var(--spacing-s);
    flex-shrink: 0;
    white-space: nowrap;
  }

  .dotted-divider {
    flex: 1 1 3rem;
    min-width: 3rem;
    max-width: 100%;
    text-align: end;
    letter-spacing: 5px;
    height: 1.675rem;
    overflow: hidden;
    color: var(--text-tertiary);
    opacity: 0.75;
  }

  .dotted-divider::after {
    content: '·····························································································································································';
    pointer-events: none;
  }

  @media (prefers-reduced-motion: reduce) {
    .related-notes-list a {
      transition: none;
    }
  }
</style>
